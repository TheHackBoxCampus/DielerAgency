type CardOwnerDescription = { text: string; question: string };

type CardOwnerSubheadline = {
  start: string;
  service: string;
  end: string;
};

export { CardOwnerDescription, CardOwnerSubheadline}
